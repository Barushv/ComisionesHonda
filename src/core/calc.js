export function getTramo(n){if(n>=11)return'11+';if(n>=8)return'8-10';if(n>=5)return'5-7';return'1-4';}
export function summarize(ventas){const totalUnidades=ventas.length;const tramo=getTramo(totalUnidades);let q1=0,q2=0;const det=ventas.map(v=>{const t={q1:0,q2:0,total:0};return {...v,...t};});return {tramo,totalUnidades,q1,q2,total:q1+q2,ventas:det};}